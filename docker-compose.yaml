# Common settings

version: '3'
services:
  editor:
    build: ./editor
    container_name: ${APP_NAME}-editor-container
    tty: true
    volumes:
      - ./editor/config/global:/root/.config/nvim
      - ./editor/config/local:/root/.config/nvim/local

# ---------- ---------- ---------- ----------
